checkWinner

    1. Guardo los indices donde tengo una "X" o "O". 
    2. Los guardo en arreglos que tienen esos indices.
        1. 2 arreglos distintos, uno para X y uno para O
        2. Junto en una funcion llamado(X) or llamado(A) entonces, winner = 
            1. estado turno, si turno == x ? 'X' : 'O'
    3. Guardar indice
        1. Elegir la casilla donde desea jugar
        2. Capturar ese indice y agregarlo a un arreglo 
        3. Hacer una funcion => sumarIndices que retorne la suma de ese arreglo

    if sumIndexs % 3 == 0 then 
        winner turn; //Gana el que tuvo el ultimo turno

1. por que no cambia el board
2. Ver quien Gana
3. Ver si hubo empate
